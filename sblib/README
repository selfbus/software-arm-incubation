Selfbus EIB Library
===================


Uses the following ARM components:

Bus access uses:
- 16bit Timer #1
- Pin 1.8 for EIB IN
- Pin 1.9 or 1.10 for EIB OUT


Test main
---------

Currently the "library" contains a test program in main.c
When a telegram is received, the LED on P0.7 flashes for 0.5sec
