Selfbus EIB Library
===================

The library uses the following ARM components:

Bus access:

- 16bit Timer #1
- Pin 1.8 for EIB IN
- Pin 1.9 or 1.10 for EIB OUT (default: 1.10)


Test main
---------

Currently the "library" contains a test program in main.c
When a telegram is received, the LED on P0.7 flashes for 0.5sec
